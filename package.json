{
  "name": "minutetrail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small, local-only Chrome website time tracker",
  "scripts": { "test": "node --test", "check": "node scripts/check.js" },
  "engines": { "node": ">=22" }
}
